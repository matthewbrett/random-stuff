{
  "name": "geo-quiz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for geo-quiz. The game itself has no build step and no runtime dependencies.",
  "scripts": {
    "build-data": "node tools/build-data.mjs",
    "serve": "python3 -m http.server 8080",
    "test": "node --test \"tools/*.test.mjs\""
  },
  "devDependencies": {
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2",
    "world-countries": "^5.1.0"
  }
}
